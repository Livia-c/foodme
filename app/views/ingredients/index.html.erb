<h2>Here are all the ingredients that you have in your Inventory</h2>
<ul>
<% @ingredients.each do |ingredient| %>
  <li><%= ingredient.category %> -> <%= ingredient.name %>: (<%= ingredient.quantity %> <%= ingredient.unit %>)</li>
  <% end %>
</ul>
