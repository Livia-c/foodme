<div class= "rounded-4 shadow-sm p-3 mb-5 bg-body rounded col-5 ">
  <p>Order number <%= order.id %></p>
  <%# check how table number will display when no table nr is given %>
  <p>Table: <%= order.table_number || "takeaway"%></p>
  <% order.order_items.each do |item| %>
    <p>Order: <%= item.menu_item.name %> (<%= pluralize @item.quantity, "portions" %>)</p>
  <% end %>
  <%= button_to "Done", order_path(order), method: :patch, class: " btn btn-primary rounded-4" %>
</div>
