<h2>Index for orders</h2>

<div class="container">
    <% @orders.each do |order| %>
      <div class="order-card">
        <%= order.table_number %>
        <h3>Order ID:<%= order.id %></h3>
        <p>Status: <%= order.status %></p>
        <p><%= link_to "Show order details", order_path(order) %></p>
        <%# <%= link_to "Delete order", order_path, data: {turbo_method: :delete, turbo_confirm: 'Do you really want to delete this unresistible order?'} %>
      </div>
    <% end %>
  <br>
  <p><%= link_to "See menu", menu_items_path %></p>
</div>
