<h2>Show selected order</h2>

<%# <div class="container"> %>
  <%# <p><%= @order.active %>
  <%# <p><%= @order.status %>
  <%# <p><%= @order.user_id %>
  <%# <%= Order.joins(:order_items).where(order_id: @order.id) %>
  <%# <br> %>
  <%# <%= link_to "Back", orders_path %>
  <%# <%= link_to "Change order", edit_order_path %>
  <%# <%= link_to "Delete this menu item", order_path(@order), data: {turbo_method: :delete, turbo_confirm: "Do you really want to delete this delicious menu?"} %>
<%# </div> %>

<div class="container">
  <ul>
  <% @order.order_items.each do |order_item| %>
    <li>Menu item: <%= order_item.menu_item.name %>/ Price: <%= number_to_currency(order_item.menu_item.price, options = {precision: 2, unit: "€"}) %>/
    <%# Quantity: <%= order_item.quantity %>/
    <%# Total Price: <%= number_to_currency(order_item.total_price) %> %> %>
    <ul>
      <li>
        <%= link_to "Add one", order_item_add_path(id: order_item), method: :post %>
        <%= link_to "Reduce one", order_item_reduce_path(id: order_item), method: :post %>
      </li>
      <li>
        <%= link_to "Remove item", order_item_path(order_item), method: :delete %>
      </li>
    </ul>
  <% end %>
</ul>
<%# <h3>Sub total: <%= number_to_currency(@order.sub_total, options = {precision: 2, unit: "€"}) %></h3> %>
<%# <%= link_to "Proceed to checkout", new_order_path %> %>
<br>
<%# <%= link_to "Empty cart", order_path(@current_order),method: :delete, data: {confirm: "Are you sure you would like to empty cart?"} %> %>
</div>
